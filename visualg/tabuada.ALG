Algoritmo "tabuada"

Var
   i: inteiro
   x: real
   r: real

Inicio

   escreval("Tabuada de qual numero?")
   leia(x)

   para i de 1 ate 10 faca
      escreval(x," x",i," =",i*x)
   fimpara


Fimalgoritmo